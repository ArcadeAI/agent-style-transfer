[tool:pytest]
asyncio_mode = auto
addopts = 
    --strict-markers
    --strict-config
    --tb=short
    --color=yes

markers =
    asyncio: mark a test as an async test
    vcr: mark a test to record API calls
